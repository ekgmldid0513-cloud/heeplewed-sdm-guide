rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 스튜디오/드레스/메이크업/헤어 사진: 누구나 볼 수 있지만(고객 페이지 노출용),
    // 업로드/삭제는 로그인된 관리자(다희님)만 가능합니다.
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
